{
  "name": "chain-gig",
  "version": "1.0.0",
  "description": "ChainGig — Decentralized freelance escrow & credentialing platform on Base L2",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:gas": "REPORT_GAS=true hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network baseSepolia",
    "deploy:mainnet": "echo 'BLOCKED: Mainnet deployment disabled until full audit. Use deploy:sepolia.' && exit 1",
    "verify:sepolia": "hardhat run scripts/verify.ts --network baseSepolia",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "keywords": ["web3", "escrow", "freelance", "base", "solidity", "sbt"],
  "author": "Francis E. Offiong",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.3.1",
    "hardhat": "^2.28.6",
    "hardhat-gas-reporter": "^2.3.0",
    "solidity-coverage": "^0.8.17"
  }
}
